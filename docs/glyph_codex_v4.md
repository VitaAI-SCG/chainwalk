# Glyph Codex v4

## Irreversibility Glyph (IRQ)

Symbol: 🟦🟧🟥⬛

Bands:
- 🟦 IRQ=reversible — exits exist
- 🟧 IRQ=primed — exits improbable
- 🟥 IRQ=irreversible — no exits
- ⬛ IRQ=floor — proof-of-work compels outcome

Usage:
- CWSPINE: `IRQ={band},{index:.2f}`
- APEX deck: "Irreversibility: {glyph} {band} — {narrative} (IRQ {index:.2f})"
- X posts: referenced when discussing optionality death

## Resolution Glyph (REI)

Symbol: 🔻

Bands:
- 🟦 REI=dormant — system can still unwind without forced resolution
- 🟧 REI=charged — resolution field is active; incentives are tilted, but paths remain
- 🟥 REI=imminent — resolution cone is narrow; regime change is near
- ⬛ REI=triggered — resolution in progress; prior regime is gone

Usage:
- CWSPINE: `REI={band},{index:.2f}`
- APEX deck: "Resolution: 🔻 {BAND} — {narrative} (REI {index:.2f})"
- X posts: referenced when you want to show that ChainWalk is tracking how the coil *actually* resolves, not just how it loads.